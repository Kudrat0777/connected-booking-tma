server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # Эта строчка направляет все запросы в index.html для правильной работы React Router
        try_files $uri $uri/ /index.html;
    }
}